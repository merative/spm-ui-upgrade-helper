// functions.ts generated by packages/code-generator/index.js

declare interface uiUpgradeHelperFunction {
  title: string,
  commandName: string,
  url: string,
}

const functions: uiUpgradeHelperFunction[] = [
{% for tool in tools %}
  {
    title: "{{tool.name}}",
    commandName: "{{tool.commandName}}",
    url: "http://localhost:{{tool.port}}/execute",
  },
{% endfor %}
];

export default functions;
