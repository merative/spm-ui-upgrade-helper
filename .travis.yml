language: node_js
node_js:
  - 12
group: bluezone
dist: xenial

env:
  - VER=`node -pe "require('./package.json')['version']"`

script:
  - yarn install
  - yarn install-all
  - yarn test
  - cd packages/gatsby-docs && yarn gatsby:deploy
